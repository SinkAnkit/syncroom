# Pinned to the exact versions verified against the full e2e suite. Open ">="
# ranges let a fresh Render build pull a breaking major release, which fails the
# build and silently leaves the previous (broken) revision live.
fastapi==0.141.1
starlette==1.3.1
uvicorn[standard]==0.52.0
sqlalchemy[asyncio]==2.0.51
aiosqlite==0.22.1
asyncpg==0.31.0
pydantic==2.13.4
python-dotenv==1.2.2
websockets==17.0
redis==8.1.0
python-jose[cryptography]==3.5.0
bcrypt==5.0.0
python-multipart==0.0.32
